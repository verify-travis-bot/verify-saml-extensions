pipeline {
    agent { dockerfile true }

    stages {
        stage('Test') {
            steps {
                sh ./gradlew --daemon --parallel test
            }
        }
        stage('Publish') {
            steps {
                sh ./gradlew --daemon --parallel publish
            }
        }
    }
}
